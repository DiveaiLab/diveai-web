# Local-only admin auth bypass.
# Copy this file to .env.local and set true when running local admin pages.
# The app ignores this bypass in production.
CMS_AUTH_BYPASS=false

# Enable only after the CONTENT_ASSETS R2 binding is available.
CMS_CONTENT_ASSETS_ENABLED=false
