# 複製這個檔案成 .env.local（不要進版控），填入你自己 Supabase 專案的值：
# Supabase 專案設定 → API 頁面可以找到這兩個值。
#
# 兩個值都留空／不建立 .env.local 的話，共學牆會自動改用瀏覽器
# localStorage 存假資料（開發階段的預設行為），不會出錯。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
